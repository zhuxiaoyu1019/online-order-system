<body class="dashboard">
    <div class="row">
        <div class="col s6 back-button">
            <a href='/pizzacutter/dashboard/product' class="btn-large waves-effect waves-light red btn tooltipped"
                data-position="bottom" data-tooltip="Product Home">
                <i class="material-icons large">chevron_left</i></a>
        </div>

        <div class="col s6">
            <a id="save-button" class="btn-large waves-effect waves-light red btn tooltipped" data-position="bottom"
                data-tooltip="Save"><i class="material-icons large">save</i></a>
        </div>
    </div>

    <h1 class="dashboard-header">Create a New Product</h1>


    <section class="section admin_edit_section">

        <div class="row input-form" id="step-1">
            <div class="row">
                <div class="input-field col s12">
                    <input id="product-name" type="text" class="validate">
                    <label for="product-name">Product Name</label>
                </div>
            </div>

            <div class="row">
                <div class="input-field col s12">
                    <input id="description" type="text" class="validate" data-length="120">
                    <label for="description">Description</label>
                </div>
            </div>

            <div class="row">
                <div class="input-field col s6">
                    <!-- Switch -->
                    <div class="switch">
                        <label>
                            Out of Stock
                            <input type="checkbox" id="in-stock">
                            <span class="lever"></span>
                            In Stock
                        </label>
                    </div>
                </div>
            </div>

            <div class="row">
                <div class="input-field col s8">
                    <i class="material-icons prefix">filter_list</i>
                    <select id="product-category">
                        {{#each categories}}
                        <option value={{id}}>{{name}}</option>
                        {{/each}}
                    </select>
                    <label>Select Category</label>
                </div>
            </div>

            <button id="upload_widget" class="cloudinary-button">Upload multiple images</button>
            {{!-- <button class="btn waves-effect waves-light" type="submit" name="action" id="submit-update"
                data-id={{product.id}}>Submit
                <i class="material-icons right">send</i>
            </button> --}}
            {{!-- <div class="row">
                <div class="input-field col s8">
                    <i class="material-icons prefix">format_size</i>
                    <select multiple id="product-size">
                        {{#each sizes}}
                        <option name="size-option" value={{name}}>{{name}}</option>
                        {{/each}}
                    </select>
                    <label>Select Sizes</label>
                </div>
            </div>
            <table class="highlight">
                <form id='sizePriceTbl'>
                    <thead>
                        <tr>
                            <th>Size</th>
                            <th>Price</th>
                            <th></th>
                        </tr>
                    </thead>
                    <tbody>
                    </tbody>
                </form>
            </table> --}}
            <button class="btn waves-effect waves-light" type="submit" name="action" id="submit-update"
                data-id={{product.id}}>Submit
                <i class="material-icons right">send</i>
            </button>
            {{!-- <div class="col s6 back-button">
                <button class="btn-floating btn-large waves-effect waves-light red basic-info"><i
                        class="material-icons large">chevron_right</i></button>
            </div> --}}
        </div>
    </section>

    <div id="step-2" class="hidden">
        {{!-- <div class="row">
            <div class="input-field col s8">
                <i class="material-icons prefix">format_size</i>
                <select multiple id="product-size">
                    {{#each sizes}}
                    <option name="size-option" value={{name}}>{{name}}</option>
                    {{/each}}
                </select>
                <label>Select Sizes</label>
            </div>
        </div>
        <table class="highlight">
            <thead>
                <tr>
                    <th>Size</th>
                    <th>Price</th>
                    <th></th>
                </tr>
            </thead>
            <tbody>
            </tbody>
        </table>
        <button class="btn waves-effect waves-light" type="submit" name="action" id="submit-update"
            data-id={{product.id}}>Submit
            <i class="material-icons right">send</i>
        </button> --}}
    </div>

    <script src="https://code.jquery.com/jquery-3.2.1.min.js"></script>
    <script src="https://cdnjs.cloudflare.com/ajax/libs/materialize/1.0.0/js/materialize.min.js"></script>
    <script src="https://widget.cloudinary.com/v2.0/global/all.js" type="text/javascript"></script>
    <script src="/js/new-product.js"></script>
</body>